# Data Intake

Use this folder for incoming examples and derived annotation packages.

- `examples/` keeps one folder per source example, including original DWG/DXF,
  preview images, notes, and annotation exports.
- `annotations/` keeps normalized label files once examples are mapped to graph
  ids.

Raw CAD files can be large or sensitive. Decide whether each incoming example
should be committed before adding it to git.

